Reads that guard a signature now require agreement between independent nodes: the app asks three in parallel and refuses to build a transaction when they disagree, instead of guessing. New app-integrity self-check hashes every file in the app and compares the result with the release anchor written on the chain. The federation page now shows the economy's real figures, read from the chain by the page itself.
